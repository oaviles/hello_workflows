name: Call Docker Container Action (.NET Core)

on: workflow_dispatch

jobs:
  action:
    runs-on: ubuntu-latest

    steps:
      - name: call netcore-action v2
        uses: oaviles/container_action@v2

      - name: call netcore-action v3
        uses: oaviles/container_action@v3
        with:
          args: '----Type_your_messsage_here_v3------'
